on:

    push:
        branches:
            - master






jobs:
    build: 
        runs-on: ubuntu-latest


        steps:
              - name: 'before'
                run: ls

              - name: 'testing printing'
                run: echo ahmed
              - uses: actions/checkout@v3

              - name: 'after'
                run: ls
              - name: "login to docker hub"
                run: docker login --username os20231592 --password dckr_pat_KvYkvfZHsls-M9V4cQvPtIGNe34